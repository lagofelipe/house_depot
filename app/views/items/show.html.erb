<h1><%=@item.item_name%><h1>

<div> <%=image_tag(@item.img_url)%></div>
price: $<%=@item.price%>.00
<%= form_for @item do |f| %>
    <%# <%= f.submit "Add to Cart" %>
    <%= button_to 'Add to Cart', new_order_path(@item.id), method: :get%>
<% end %>
<%# 
if item.most_popular_item
<%puts @item.item_name is a bestseller%>