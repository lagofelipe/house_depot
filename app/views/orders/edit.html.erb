<h2>Edit your order<h2>

<%= form_for @order do |f| %>

 
  
  <%= f.collection_select :item_ids, Item.all, :id, :item_name %>
  <br/>
  <br/>

  <%= button_to 'Add to Cart', orders_path(@order.id)%>
<% end %>